import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "Proyecto1",
  webDir: "dist", // Carpeta donde se compilan los archivos
  bundledWebRuntime: false,
};

export default config;
